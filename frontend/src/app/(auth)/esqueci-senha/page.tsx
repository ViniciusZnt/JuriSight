import { ForgotPasswordPage } from "@/components/login/forgot-password-page";

/** Recuperação de senha — fora da casca autenticada (sem sidebar). */
export default function EsqueciSenha() {
  return <ForgotPasswordPage />;
}
