import { SearchResultsPage } from "@/components/resultados/search-results-page";

/** Resultados da busca (Figuras 4-5 da RFC). */
export default function ResultadosPage() {
  return <SearchResultsPage />;
}
